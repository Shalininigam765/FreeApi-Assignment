# Authentication App

This is a simple authentication application built using React. It allows users to register and log in using their email and password. The application uses local storage to save user data and manage authentication state.
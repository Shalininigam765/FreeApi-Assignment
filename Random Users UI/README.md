# Random Users UI

This project is a simple user interface that displays random user information fetched from the Random User API. It allows users to view details such as name, email, and profile picture of randomly generated users.